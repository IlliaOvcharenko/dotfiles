- defaults:
    link:
      relink: true
      force: true

- clean: ['~']

- shell:
  - [git submodule update --init --recursive, Installing submodules]

- link:
    ~/.config/nvim: nvim
    ~/.vim: vim/vim
    ~/.vimrc: vim/vimrc
    ~/.tmux.conf: tmux/tmux.conf
    ~/.bash_aliases: bash/bash_aliases
    # ~/.local/bin/start-tmux: tmux/start-tmux.sh
    # ~/.local/bin/stop-tmux: tmux/stop-tmux.sh




